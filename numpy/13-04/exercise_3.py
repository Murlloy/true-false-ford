import numpy as np

array = np.array([[1, 2, 3], [4, 5, 6]])

vetor_add = np.array([10, 20, 30])

soma = array + vetor_add
multiplicacao = soma * 2

 
print(f"Matriz Inicial: {array}")
print(f"Matriz multiplicada: {multiplicacao}")
print(f"Matriz mult: {soma}")
